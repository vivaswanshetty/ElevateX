# Server Configuration
NODE_ENV=development
PORT=5000

# Database
MONGO_URI=mongodb://localhost:27017/elevatex
# For production, use MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/elevatex?retryWrites=true&w=majority

# JWT Secret (change this to a random string in production)
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Email Configuration
# For development, you can use Ethereal Email (automatically creates test accounts)
# For production, use a real email service like Gmail, SendGrid, or AWS SES

# Gmail example:
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=465
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-specific-password

# SendGrid example:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASSWORD=your-sendgrid-api-key

EMAIL_FROM=noreply@elevatex.com
SUPPORT_EMAIL=support@elevatex.com

# Rate Limiting (optional - defaults are set in code)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
